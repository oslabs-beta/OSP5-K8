import React from "react";
import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";
import App from "../../components/App";

test("renders NavBar", () => {
  // render(<App />);
  // render(<App />);
  // const child = screen.getByText(/NavBar/i);
  // expect(child).toBeInTheDocument();
});
